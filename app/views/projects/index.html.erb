<p>DevPoint Labs coders are constantly working on projects to 1UP their 
coding-ninja skills!
</p>

<p>To help them manage all of these projects, they have 
created this project management application to keep track of their current 
progress and remaining tasks. 
</p>
<br>

<table class='table table-hover'>
  <thead>
    <tr>
      <th>Project Name</th>
      <th>Description</th>
      <th>Due Date</th>
      <th>Owner</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @projects.each do |project| %>
      <tr>
        <td><%= link_to project.name, project %></td>
        <td><%= project.description %></td>
        <td><%= project.due_date_at.localtime.strftime("%A, %b %-d, %Y  %l:%M%p") %></td>
        <td><%= project.creator.name %></td>
        <td><%= link_to 'Edit', edit_project_path(project), class: 'btn btn-xs btn-default' %></td>
        <td><%= link_to 'Delete', project, class: 'btn btn-xs btn-default', method: :delete, 
                                            data: { 
                                              confirm: 'Are you sure you want to delete this project?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

